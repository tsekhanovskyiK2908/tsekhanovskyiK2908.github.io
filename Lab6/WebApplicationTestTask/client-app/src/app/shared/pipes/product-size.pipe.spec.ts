import { ProductSizePipe } from './product-size.pipe';

describe('ProductSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductSizePipe();
    expect(pipe).toBeTruthy();
  });
});
