import { EnumConverterPipe } from './enum-converter.pipe';

describe('EnumConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
